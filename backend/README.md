Backend (Express + MongoDB)

.env.example contains env vars. Install and run:

npm install
copy .env.example to .env and fill values
npm run dev

API endpoints:
- POST /api/auth/register
- POST /api/auth/login
- GET/POST/PUT/DELETE /api/tasks (protected)
